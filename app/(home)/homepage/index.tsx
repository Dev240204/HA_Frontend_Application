import React from 'react'
import { ScrollView, Text, View } from 'react-native'

const index = () => {
  return (
    <ScrollView>
      <View className='flex justify-center items-center'>
        <Text className='text-2xl'> Index Page for Home </Text>
      </View>
    </ScrollView>
    )
}

export default index